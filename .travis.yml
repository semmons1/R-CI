language: r
cache: packages
r:
  - release
sudo: required

env: _R_CHECK_CRAN_INCOMING_=FALSE

r_packages:
  - covr
  - devtools
  
r_github_packages:
  - semmons1/R-CI

script:
  - Rscript test.R

after_success:
  - Rscript -e 'library(covr); codecov()'
